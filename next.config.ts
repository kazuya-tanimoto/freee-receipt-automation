import type { NextConfig } from 'next'

/**
 * Next.js configuration for freee Receipt Automation
 * - Minimal configuration following TooMuch avoidance principle
 * - Turbopack enabled via CLI flag (--turbo)
 */
const nextConfig: NextConfig = {
  // Minimal configuration - Turbopack is enabled via CLI flag
}

export default nextConfig
